template PlaceOrder(balanceLevels, accountLevels) {
	// TODO: check signature

	// order1_tokensell != order1_tokenbuy;
	// order1_amountsell > 0;
	// order1_amountbuy > 0;

	// order2_tokensell != order2_tokenbuy;
	// order2_amountsell > 0;
	// order2_amountbuy > 0;
}